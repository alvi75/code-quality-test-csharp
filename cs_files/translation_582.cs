using System;
using System.Collections.Generic;

public class Translation582<T>
{
    public HostedZone(string id, string name, string callerReference){
    _id = id;
    _name = name;
    _callerReference = callerReference;
}
}