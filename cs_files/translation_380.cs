using System;
using System.Collections.Generic;

public class Translation380<T>
{
    public ReplaceableAttribute(string name, string value, bool replace){
    _name = name;
    _value = value;
    _replace = replace;
}
}