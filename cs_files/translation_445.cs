using System;
using System.Collections.Generic;

public class Translation445<T>
{
    public virtual void Run(){
    DoRefreshProgress(mId, mProgress, mFromUser, true);
    mRefreshProgress = null;
}
}