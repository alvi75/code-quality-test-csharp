using System;
using System.Collections.Generic;

public class Translation146<T>
{
    public void UnwriteProtectWorkbook(){
    records.Remove(fileShare);
    records.Remove(writable);
    records.Remove(protect);
    fileShare = null;
    writeable = false;
    protect = false;
}
}