using System;
using System.Collections.Generic;

public class Translation580<T>
{
    public static void Register(IMergeStrategy imp){
    if (registry.ContainsValue(op)){
        throw new ArgumentException("merge operator type already exists");
    }
    registry[op.Key] = op;
}
}