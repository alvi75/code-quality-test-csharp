using System;
using System.Collections.Generic;

public class Translation924<T>
{
    public ListSubscriptionsByTopicRequest(string topicArn, string nextToken){
    _topicArn = topicArn;
    _nextToken = nextToken;
}
}