using System;
using System.Collections.Generic;

public class Translation147<T>
{
    public SolrSynonymParser(bool dedup, bool expand, Analyzer analyzer): base(dedup, analyzer){
    this.expand = expand;
}
}