using System;
using System.Collections.Generic;

public class Translation615<T>
{
    public Distribution(string id, string status, string domainName){
    _id = id;
    _status = status;
    _domainName = domainName;
}
}