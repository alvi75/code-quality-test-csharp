using System;
using System.Collections.Generic;

public class Translation712<T>
{
    public TimerThread(long resolution, Counter counter): base(ThreadName){
    this.m_resolution = resolution;
    this.m_counter = counter;
}
}