using System;
using System.Collections.Generic;

public class Translation514<T>
{
    public XPath(Parser parser, string path){
    this.parser = parser;
    this.path = path;
    this.elements = Split(path);
}
}