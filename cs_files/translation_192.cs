using System;
using System.Collections.Generic;

public class Translation192<T>
{
    public KeySchemaElement(string attributeName, KeyType keyType){
    _attributeName = attributeName;
    _keyType = keyType;
}
}